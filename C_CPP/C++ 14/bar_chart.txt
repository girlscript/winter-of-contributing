/*To create a bar chart made of '*' for comparing population using array in c:-
In this we will create the chart using 2D array.*/
#include<stdio.h>
int main()
{
    int arr[20],i,n,j,max;
    printf("enter no. of countries");
     scanf("%d",&n);
     printf("enter the population in billion of countries:");
    for(i=0;i<n;i++)//first of all we will enter the population in billions in each element of array
    {
        scanf("%d",&arr[i]);
    }
    max=arr[0]; 
    for(i=1;i<n;i++)
    {
        if(max<arr[i])
            max=arr[i];
    }
     for(i=max;i>=1;i--) //we will find maximum first, which will denote each row and it will keep on decreasing as we move down in row.if row no(max) i.e.i is less than or equal to respective element of array then print * else keep space and follow this procedure in each line ,we will get chart.
     {for(j=0;j<n;j++){
            if(arr[j]>=i)
            printf("*\t");
     else
        printf("\t");
        }
        printf("\n");
        }


    return 0;
}
//Similarily,we can create comparison based on productivity,salary etc.according to our requirement.
